# 航迹起始 | TrackInit - 多目标



# 计划
最终封装为一个完整的类，支持事务处理，能够接收一批点迹数据，输出一批航迹起始信息。支持多种算法选择和参数配置。


## 软件框架
### 1. 结构体及不可变参数定义
  - def_init.h 用于定义错对外结构体，系统参数等
### 2. 抽象层
  - TrackInitBse 用于定义所有航迹起始组件的接口
### 3. 组件层
   - HoughSlice 基于DBSCAN的霍夫变换算法，将目标区域基于聚类切片，对每个聚集区域，基于霍夫变换执行航迹起始
### 4. 工具层
   - ObjectPool 通用对象内存池
   - Func_dbscan 聚类算法实现 
### 5. 服务层
   - InitService 航迹起始服务层，占据一个单独的线程，调用接口对外提供服务

# 开发日志
## 2025-12-07 至 2025-12-11
1. **创建def_init.h，用于定义基础数据结构和参数**
2. **SingleTargetMemoryPool功能测试完成**
   - 取代malloc/free，快速分配内存
   - 分配10000个目标：平均6.88303 ms
   - 重用1000次：平均776.633 us

## 2025-12-11 至 2025-12-18
1. **HoughSlice算法核心实现完成**
   - 实现了TrackInitBase抽象基类接口
   - 集成DBSCAN聚类算法用于点迹关联
   - 完成内存池DetectedArea集成，支持动态聚类分配
2. **霍夫变换框架搭建**
   - 定义Slice结构体，支持4批次历史点迹存储
   - 实现vote_area二维数组(角度×距离)用于霍夫投票
   - 配置化参数系统：角度分辨率3°，距离分辨率1km，搜索半径50km
3. **状态码系统设计**
   - ProcessStatus枚举：SUCCESS、POINTS_TOO_DISPERSED、TOO_MANY_CLUSTER等
   - 支持算法链式调用：失败状态码(1001-1100)可切换其他算法
4. **引入AI辅助测试**
   - 创建.github/copilot-instructions.md AI开发指南

## 2025-12-18 至 2025-12-22
1. **完善HoughSlice算法**
   - 引入了github上的项目https://github.com/eleobert/dbscan，依据项目需求对其进行了修改，通过dbscan函数即可调用
2. **两类HoughSlice聚类的处理时间比较**
   - 对比两类聚类的处理时间差异：先判断点迹是否在聚类中心附近再聚类，和先聚类再合并聚类的方法
   - 分别设置为gen_1和gen_2两套算法，回学校了在测试

## 2025-12-22 至 2025-12-26
1. **SingleTargetMemoryPool修改为ObjectPool**
   - 原先的单目标内存池过于臃肿，修改为通用对象池ObjectPool<T>重新进行测试，剔除了handler等不必要的功能，不再支持自动析构
   - 使用deque+unordered_set实现空闲对象管理，适配刚需扩容的场景，但是速度较vector慢
   - 目标分配测试:
     |          benchmark name          | est run time |    mean    |  low mean  | high mean  |  std dev   | low std dev | high std dev |
     | :------------------------------: | :----------: | :--------: | :--------: | :--------: | :--------: | :---------: | :----------: |
     |  容量为1的对象池扩容至10k-测试   |  33.1548 ms  | 330.968 us | 327.947 us | 334.005 us | 15.4725 us | 13.7663 us  |  17.6859 us  |
     | 10k对象的池反复获取释放-强度测试 |  14.292 ms   | 141.74 us  | 140.802 us | 143.074 us | 5.66977 us | 4.13455 us  |  8.27005 us  |
     |   10k对象的池获取指针速度-测试   |  26.5129 ms  | 271.204 us | 265.456 us | 293.216 us | 51.2152 us | 10.7818 us  |  119.695 us  |
2. **HoughSlice算法实现**
   - 粗糙的比较了两类新建聚类的方法，发现先聚类再合并的方法在点迹分布均匀的情况下效果更好，但是对于不同情况还需要进一步测试

## 2025-12-27
1. **Release模式性能测试完成**
   - 使用Catch2基准测试比较gen1和gen2聚类算法性能
   - 测试场景：完全随机点迹、五个均匀/高斯/瑞利分布聚类
   - 结果显示gen1算法（先检查聚类中心再聚类）在聚类场景下性能优于gen2（先聚类再合并），平均运行时间gen1 ~250us vs gen2 ~1.5ms
   - 随机场景下两者性能相近，gen1 ~190us, gen2 ~198us  

## 2026-01-07
1. **完善HoughSlice峰值过滤器**
   - 修改peak_filter函数逻辑，确保只有当所有批次的值均非零时，才计算并返回最小峰值数量
   - 修改了process函数，约束了航向角范围，使其符合速度的物理限制
   - 准备完成TODO中的内容，增加角度和索引转换函数，以及航向判断逻辑

## 2026-01-17
1. **HoughSlice流程打通（聚类→投票→峰值→回溯）**
   - 在 [src/HoughSlice.cpp](src/HoughSlice.cpp) 完成 `process()` 主流程串联：聚类生成、批次点迹投票、峰值提取与回溯生成4点段航迹。
   - 仅在 `current_batch_index == 3` 的切片上执行峰值与回溯，保证4批点迹满足后再输出航迹。
2. **霍夫投票区间与多普勒离散编码**
   - 基于 `doppler/VELOCITY_MAX` 计算允许航向夹角 `±acos(|v|/vmax)`，以点相对聚类中心方位 `beta` 为中心构造区间，并处理跨 0/2π 的两段情况。
   - 每批次用16位保存速度位：负速映射到 bits 15..8，正速映射到 bits 7..0（静止不计入）。投票时对 `(θ, ρ)` 单元做位或，分批次左移 16 位。
3. **峰值一致性与直线参数恢复**
   - `peak_filter()` 采用四批次16位按位与，只有四批共同速度位才视为有效峰值，返回位掩码。
   - `process_extract_peak_from_hough_space()` 逐位还原 `doppler`，并由索引恢复 `theta`、`rho`，输出候选直线参数集合。
4. **点迹回溯与容差**
   - `process_backtrack_points()` 按候选直线在四批各自点集中搜索匹配点；使用 `RHO_TOL=HOUGHSLICE_RHO_RESOLUTION_KM/2` 与 `DOPPLER_TOL=1.0` 做几何/速度一致性判定，成功则输出4点段航迹。
5. **TODO / 明日计划**
   - 发送航迹：完成 `process()` 中“发送航迹 // TODO”的通信/回调对接。
   - 补充测试：构造含4批点迹的单元/基准测试（含跨 0/2π 航向、正/负多普勒、边界ρ索引等场景）。

## 2026-01-18
1. 霍夫变换切片删除逻辑完善
   - 在 `process()` 函数中，完成对已经执行过航迹生成的切片的删除，确保内存池资源得到有效回收。
2. 霍夫变换区域修改
   - **角度搜索范围修改**
   - 目前把角度的搜索给改了。因为doppler的引入，航向角变成了射线而非直线，所以把角度范围从0-360度改为了0-180度。
   - 现在有点后悔，觉得不如doppler按绝对值写比较好，角度还是保持360度。
   - 上面那句话不对，角度还得是180吧，物理意义明确，能保证原空间和霍夫变换空间一一对应。
   - 那就180定下了，doppler还是保留正负，为了*保证原空间和霍夫空间一一对应*，且内存利用率高
   - **截距范围修改**
  - 截距范围做了修改，由于聚类的引入，实际上区间应该是`2*根号2*RADIUS`。
  - 怕取整和计算问题，干脆扩大了一倍，变成了`4*RADIUS`。
  - 目前只修改了参数定义，后续还需要修改索引转换函数。
  - 索引是从0开始的，而截距是从负值开始的，工应该主要是修改这部分内容
3. **TODO / 明日计划**
   - 截距索引转换函数的重写
   - 位扩展类的实现
   - 昨天的发送航迹功能还没实现。。需要决策下实现方式，一定得去耦合：“发送航迹：完成 `process()` 中发送航迹TODO部分”
   - 单元测试也没写，至少得验证转换函数的功能是否正确，性能最后用GPU去优化比较好

## 2026-01-19 
1. **HoughSlice截距索引转换函数重写**
   - 完成了截距索引与实际值的转换函数，确保索引从0开始，实际截距范围为[-2R, +2R]。
   - 修改了 `process()` 中相关计算逻辑，确保投票和峰值提取使用新的索引映射。
2. **⚠位数改动类**
   - 我想着把doppler的位数从uint64_t改成能自由变长的类，但是和uint64_t这些兼容性不太好，改动太大
   - 记录一下，以后要修改的话需要修改的地方：
     - vote_area的定义
     - 投票的位运算 process_vote_points_in_cluster()
     - 峰值提取的位运算 peak_filter()

## 2026-01-22 至 2026-01-30
1. **HoughSlice发送航迹功能实现**
   - 在 `process()` 函数中完成航迹发送逻辑，使用回调函数 `track_callback_` 将生成的航迹数据传递给外部系统。
   - 已经观测到回调函数被正确调用，航迹数据（观测到航迹成功绘制）
   - 修改了trackvisualizer的结构，现在是clear->draw->creat->add->merge的优先级顺序，避免乱位
2. **HoughSlice单元测试编写**
   - 还在功能性测试阶段，聚类进行了修改，现在对于不归属于任何聚类的点迹，会生成一个新的聚类
   - 现在process_cluster_generation在单点情况下已经完成了测试
   - process_point_for_hough_vote部分已经定位到bug，目前投票情况是一票没投。。用MATLAB写了个小程序验证了霍夫变换的正确性，发现代码里投票逻辑有问题
   - 后面两部分还没验证，明天继续
3. **Func_dbscan函数逻辑修改**
   - 修改了dbscan函数的逻辑，现在对于不归属于任何聚类的点迹，会生成一个新的聚类
   - 这样可以避免点迹被丢弃，确保每个点迹都能参与后续的霍夫投票
   - 然后被使用的点应该是不能被第二个聚类使用。。。。不过使用了也许也行，反正是两个聚类切面
4. **TODO / 明日计划**
    - process_point_for_hough_vote的一票未投需要修改
    - 位扩展类不写了，最近几天先尽快完成这个航迹起始算法的开发和测试

## 2026-02-18
1. **HoughSliceBUG修复**
   - 修复了 `process_point_for_hough_vote()` 中的投票逻辑错误，主要是由于最大速度设置过小导致的
   - 修改了`process_point_for_hough_vote`中的逻辑错误，原代码混淆了 point.xy 和 rel_x, rel_y 的使用
   - MATLAB对应代码的读取错误是由于维度不匹配导致的。。并非弧度，虽然看上去结果很像是维度导致的
   - 修改了当点迹依据DOPPLER推算的角度在合理范围内时，反而会导致投票空间越界的BUG，是由于此时的角度区间会越过180度导致的
   - 存在的问题：霍夫空间中监出点迹过多（大幅提高距离分辨率可以有效改善该问题）。。即便是单个航迹迹也能监测出来大量的航迹（存在重复计数），计划在反向索引的时候，每次完成一条反向索引就删除所有的相关点迹，避免重复索引导致的错误，不过这样也有可能导致错误的航迹侵占正确航迹的点迹，导致正确航迹无法被索引出来。。需要进行对比实验验证一下
   - 存在的问题： 霍夫变换采用过大分辨率会导致数据量爆炸，而且目前尚且没明确增加角度分辨率能否进一步剔除无用点迹，需要进行对比实验。。总体来说我觉得霍夫变换可能不是很可行了，后续我还是希望MHT算法能完成
2. 完善了MATLAB脚本，后续合并到项目中，作为单元测试的一部分，验证整个流程的正确性和性能表现
3. **TODO / 明日计划**
   - 反向索引完成后删除相关点迹，避免重复索引导致的错误
   - 继续完善单元测试，构造包含4批点迹的测试场景，验证整个流程的正确性和性能表现

## 2026-02-19
1. **BitArray类引入**
    - 一个变长类，用来替代uint64_t实现任意位宽的位数组，主要用于霍夫变换中多普勒速度的存储
    - 支持基本的位操作（与、或、非、左移、右移）以及位访问和设置
    - 添加了测试代码，进行了功能测试和性能测试
    - **性能基准**（Release模式，Catch2，100 samples，数据规模：批量字节=128KiB，位/移位/逻辑/拷贝=64KiB）：
      | 操作类型                  |    均值    |   标准差   |     吞吐量     |
      | :------------------------ | :--------: | :--------: | :------------: |
      | **批量写入** (131072字节) | 90.013 µs  |  2.359 µs  | **1.39 GiB/s** |
      | **批量读取** (131072字节) | 51.474 µs  |  1.999 µs  | **2.43 GiB/s** |
      | **设置所有位** (524288位) | 330.506 µs |  5.359 µs  |       -        |
      | **读取所有位** (524288位) | 173.529 µs |  4.438 µs  |       -        |
      | **左移 1 位**             |  7.914 µs  |  0.825 µs  |       -        |
      | **左移 32 位**            |  2.173 µs  |  0.326 µs  |       -        |
      | **右移 1 位**             |  7.537 µs  |  0.371 µs  |       -        |
      | **右移 32 位**            |  2.169 µs  |  0.127 µs  |       -        |
      | **位与操作**              | 970.475 ns | 123.557 ns |       -        |
      | **位或操作**              | 946.663 ns | 70.246 ns  |       -        |
      | **拷贝构造**              |  0.188 ns  |  0.012 ns  |       -        |
      | **赋值操作**              |  0.188 ns  |  0.011 ns  |       -        |
2. **HoughSlice精度改进**
   - 将速度存储位宽从 16 位扩展至 256 位（每批次 64 位），速度分级从 ±8 级提升至 ±32 级（引入了bitarray和相关宏，可以自定义）
   - 优化了反向索引逻辑，显著减少了速度索引错误
   - 发现增加角度分辨率只会增加霍夫空间的维度，并不会剔除无用点迹，只会让无用点迹更多，急需要一种方法来剔除无用点迹，靠反向索引是行不通的，必须得依赖霍夫空间点迹凝聚，所以把DOPPLER的位宽增加了
   - 增加RHO分辨率抑制效果很好，不过最好还是依赖于增加批次数，这样也能适当利用霍夫变换本身的性质
   - 准备增加批次数。。
3. **TODO / 明日计划**
  - 反向索引完成后删除相关点迹，避免重复索引导致的错误（待定，我感觉这样太草率了，可能分配到不优秀的航迹里面去反而导致点迹出不来，尤其是两篇HOUGHSLICE重合的时候）
  - 继续完善单元测试，构造包含4批点迹的测试场景，验证整个流程的正确性和性能表现
  - 优先：批次数可以自定义的功能还是得提上来，我认为有了这个能大大压缩点迹数量，不过这样很多地方的逻辑可能要改，显得不像是霍夫变换了

## 2026-02-21
1. **HoughSlice点迹凝聚实现**
   - 关于19日提到的“反向索引完成后删除相关点迹”，已经改成了使用点迹凝聚的思路，故此不在实现“反向索引完成后删除相关点迹”功能
   - 实现了基于角度和距离索引的点迹凝聚算法，将霍夫空间中的相邻峰值合并为一个候选直线参数
   - ⚠19日发现的“增加RHO分辨率抑制效果很好”，主要是抑制了THETA的扩散，并非是RHO有扩散
   - THETA的减小可以有效降低不明点迹的出现
   - 通过调整 `THETA_CLUSTER_TOL` 和 `RHO_CLUSTER_TOL` 参数，可以控制凝聚的严格程度，减少无用点迹的影响
   - 凝聚后的候选直线参数更稳定，反向索引时更容易找到匹配的点迹，提高了航迹生成的准确性
2. **HoughSlice点迹回溯优化**
   - 速度容差不再是绝对值，而是受到VELOCITY_MAX的控制、根据速度分辨率动态计算，确保不同速度级别的点迹都能得到合理的匹配
3. **HoughSlice航迹生成所需批次数可配置**
   - 19日”批次数自定义功能已经是心啊了“，航迹生成所需的点迹批次数现在受宏控制，支持使用K（K>4）批次的点迹数据来生成航迹
   - 通过调整 `HOUGHSLICE_BATCH_NUM` 宏定义，可以灵活配置生成航迹所需的点迹批次数，大于4时精度提升
4. **TODO / 明日计划**
   - THETA的扩散问题需要进一步的研究，可以进行参数扫描
   -  `THETA_CLUSTER_TOL` 和 `RHO_CLUSTER_TOL`必须受到宏的控制，绝对不能是写死的，会存在今天点迹回溯中出现的适应性问题，进一步提高分辨率可能会导致凝聚效果不理想，降低分辨率又会导致过度凝聚，所以必须受"分辨率宏"的控制
   -  单元测试需要增加更高批次点迹的测试场景，验证批次数提高的效果和性能表现
   -  进一步增加测试点迹，研究边界情况和参数敏感性，确保算法的鲁棒性和适用范围
   -  max_velocity过小的时候存在BUG，部分投票出现在了不可能出现的角度上，需要引起重视

## 2025-02-24
1. **HoughSlice点迹凝聚超参数调整**
   - 优化了点迹凝聚的超参数，确保 `THETA_CLUSTER_TOL` 和 `RHO_CLUSTER_TOL` 受宏控制，并且能够根据分辨率动态调整
   - 解决了航迹回溯的时候存在的点迹搜索不到的问题（两处门限不匹配）
2. **雷达站近距离无峰值检出问题部分修复**
   - 距离雷达站较近的目标DOPPLER速度变化较快，由于不是使用传统的直线投票检出，所以出现了峰值无法检出的问题，现在通过最大速度对该问题进行了一定的改善 
   - 但是DOPPLER投票逻辑显然出问题了，所以明天还得修BUG
3. **TODO / 明日计划**
   - 点迹回溯必须得增加删除机制，不能点迹复用了
   - 增加测试点迹，杂波点迹，准备首位
   - DOPPLER扩展位引入带来了全新的BUG准备修一下

## 2026-02-25 至 2026-02-27
1. **HoughSlice DOPPLER投票逻辑修复**
   - 修复了DOPPLER投票逻辑中的错误，确保根据点迹相对于聚类中心的方位和最大速度计算正确的投票区间，并正确处理跨 0/2π 的情况
   - 现在DOPPLER投票逻辑已经能够正确地将点迹投票到对应的角度和距离单元中
   - 修改了另一处错误，DOUBLE-SIZE和SIZE-DOUBLE不匹配的问题，DOPPLER位现在已经可以确认是匹配的了
2. **HoughSlice点迹回溯逻辑修复**
   - 随机数种子为42，点迹数为3个点的时候，存在峰值检测出来了，但是找不到对应的点迹的问题，需要予以修复。。
   - 👆HEADING转换成THETA的计算逻辑存在BUG，已经修复
   - 随机数种子为42，点迹数为10个点的时候，存在峰值检测不出来的问题，
   - 👆两个原因
     - 经过检查时对于THETA忘记取法线方向导致的
     - 没有进一步排除，但是发现了当使用聚类的时候，峰值能够正常检测出来了，RHO的聚类提高到0.5立刻可以获得一个结果。。也可能是RHO在转换的过程中出现了纰漏
     - 进一步的，通过增加聚类的大小，现在有效遏制了航迹拥簇问题，航迹数量已经完全正常了
3. **TODO / 明日计划**
   - 点迹回溯删除机制。。已经写了一部分了，感觉还是可以有的
   - 增加测试点迹，杂波点迹。。堆数量已经完成了，把杂波点加上去看看效果咋样，再用MATLAB写个对照的就完事
   - 详细测试下各个参数的影响。。反正我发现聚类参数现在能够有效解决航迹拥簇问题，距离分辨率的提高会急剧降低点迹检出的概率，可能是由于THETA分辨率不高引起的错位，所以RHO分辨率太高也不好，角度分辨率的提高效果倒是不错，。。效果一直很好，就是vote_in_hough_space函数的四舍五入逻辑要改改，明天得详细研究下增大边界值的影响，现在用的硬编码，理论上其实都不应该加。。。
   - 把推导的公式记录一下。。反正也挺简单

## 2026-02-28
1. **HoughSlice点迹回溯机制不添加**
   - 航迹融合由航迹管理来完成，反正AIS直接检索出来的航迹也要和现在的航迹合并的，每次生成航迹的时候合并下得了
2. **doppler搜索方法改进**
   - 现在依据theta,x,y,doppler计算出doppler的搜索范围，减弱了航迹拥簇问题，同时可以计算出SOG和COG，从而提高精度
3. **测试结果**
   - 经过实验验证，该算法对于杂波点的抗性相较于传统算法有显著提升，当前算法假定一个区域内的杂波点数量有上限，在上限杂波点数量下，该算法仍然能够正确成航迹
   - 但是该算法对于误差的容忍度极低，对于以100m/s为均值，标准差为50m/s的物体，当x,y的标准差达到300m时候几乎没有航迹出现，对于doppler的标准差容忍度尚可（来源于doppler_tolerance带来的DOPPLER噪声容限），峰值能够正常检测，回溯结果页正常，大约在0.8m/s的标准差时，航迹显著消失。降低多普勒分辨位数会有效降低这个阈值
   - 结论：该算法是应用于4批次出航迹的霍夫变换算法，适用于点迹分布范围广，且具备局部聚集性，速度较快的目标
   - 该算法的瓶颈在于
     - 1. 过分约束了航迹的参数，导致对噪声容忍度过低，以及聚类算法的引入带来的不可避免的矛盾：航迹检测不出或者航迹拥簇，本质上还是批次数量过少导致的，我认为批次数量较少的时候不适用霍夫变换。
     - 2. 航迹回溯的时间复杂度过高，几乎接近逻辑法了，以及约束部分占用了过长的时间。。将VOTE部分的约束剔除后效果有明显改善，对杂波的抗性未观测到明显下降，以后GPU化可以考虑对此部分内容进行优化
     - 未使用GPU，时间性能表现不佳，平均每批次处理时间在几十毫秒到几百毫秒不等，后续可以考虑GPU化以提升性能
  - CPU版本BENCHMARK结果（Release模式，Catch2，100 samples）：
4. **TODO / HOUGHSLICE剩余所有工作**
   - 未来有空再改，纯为了美观
   - 准备改DOPPLER容忍度从size_t变成double，实现权责分离(⚠️)

## HOUGHSLICE最终结果记录
<!-- 让表格居中显示的风格 -->
<style>
.center 
{
  width: auto;
  display: table;
  margin-left: auto;
  margin-right: auto;
}
</style>

<div class="center">

| 指标              | 平均值 | 标准差 |
| :---------------- | :----: | :----: |
| **处理时间 (ms)** | 82.40  |  3.89  |
| **检测率**        | 82.0%  |   -    |
| **虚警率**        | 14.51% |   -    |
| **航迹数**        | 54.20  | 44.24  |

</div>

<p align="center"><font face="黑体" size=2.>表1 抗杂波能力测试结果</font></p>

**测试配置：** 100轮测试，每轮10个目标点迹 + 40个杂波点/帧，共处理4帧数据

## 2026-03-01
1. **HoughSlice算法衔接论文，写了一个总结**
2. 逻辑法建了一个HPP文件，一会吃饭后写具体实现
3. **LogicBasedTracker 核心设计完成**
   - 设计了一个基于四帧滑动窗口和多假设跟踪(MHT)思想的航迹初始化算法
   - 采用四层结构：point_batches_存储点迹观测，hypothesis_tree_存储假设节点，始终保持[0]为最新帧
   - 定义了HypothesisNode结构体，包含航向假设(heading)、径向速度范围(vr_min/vr_max)、节点深度(depth 0-3)、关联点迹指针(obs)和父节点指针(ancestor)
   - 处理流程
      - 第一帧：无历史匹配，每个点迹生成 depth=0 的根节点，初始化多普勒预测范围
      - 第二/三帧：新点迹遍历上一帧节点，通过距离门限和多普勒门限匹配，生成 depth 递增的子节点
      - 第四帧：关键修正——depth=2 的节点匹配成功后不生成 depth=3 节点，而是直接进入航迹输出流程
      - 每帧处理完成后执行滑动窗口右移，清空最旧层数据
      - 航迹输出流程：回溯点迹，依据近似静止目标和运动目标两种情况计算航向和速度，输出航迹信息
         - 近似静止目标：当四帧多普勒累积位移 |doppler| * dt_total < sigma_dis 时，航向垂直于视线方向，速度取多普勒绝对值
         - 运动目标：否则用 SVD 拟合直线得到航向，结合多普勒解算真实速度
      - 关键技术参数
         - 距离门限：MAX_VELOCITY * Δt + sigma_dis + dis_tolerance，其中 sigma_dis 网格化在线标定
         - 多普勒预测：以靠近雷达站运动的目标为例，·一个x,y往heading方向移动的点迹，如果HEADING不巧是沿着垂直于视线方向移动的话，他的doppler会剧烈变动，HEADING最接近垂直于视线方向的时候一定是我们假设他是MAX_VELOCITY的时候（固定值），此时我们依据TIME_1和TIME_0计算出最大移动距离，如果这个移动距离小于hypot(x,y)cos(aspectAngle)，那么DOPPLER_MIN就是0，不然则依据最大移动距离后的点计算DOPPLER_MIN，DOPPLER_MAX就是他自己
      - 后处理聚类：当单网格内假设过多时，对航向和速度聚类，取最稠密 K 个簇输出
4. **TODO/明日计划**
   - 需要一个固定大小，不扩容的对象池来存储HypothesisNode，支持快速分配和回收，
   - python的svd分解转换成CPP
   - 首要：实现一个基础的假设构建函数extend_hypotheses，实现一到四帧的假设推演，当depth=3的目标将要出现时，取消出现，调用output_hypotheses判断航迹是否满足条件，输出为正式航迹

### 2026-03-02 至 2026-03-04
1. **LogicBasedTracker核心功能实现**
   - 基础逻辑：拆分为hypothesis索引，hypothesis推导，和航迹质量判断两个核心部分
   - 部分完成了hypothesis索引的实现，找到了波门大小，缺少依据DOPPLER反推可能的波门的步骤，我标记了TODO，
   - 选取x,y作为hypothesis的索引，因为theta和r需要引入sog_cog来确定搜索范围，x,y则不需要
   - 引入第二章准备计算的偏差和方差，从而优化假设索引的范围）（感觉可能还是得进一步扩大，之后再试试）
2. **LogicBasedTracker误差分布函数初始化实现**
   - 依据基础的THETA,RHO误差分布函数，结合SOG/COG的误差分布，计算出x,y的误差分布函数
   - 通过网格化的方式，计算出不同x,y位置的误差分布函数，存储在二维数组中。使用DAT文件测试，用MATLAB验证了误差分布函数的正确性
   - 验证下SIGMA_X,SIGMA_Y的分布为啥仅与一个变量有关
3. **TODO / 明日计划**
   - 继续完善hypothesis索引的实现，增加DOPPLER反推可能的波门的步骤，我标记了TODO
   - 实现hypothesis推导函数extend_hypotheses，实现一到四帧的假设推演，尤其是需要引入DOPPLER来进行减枝，此外当depth=3的目标将要出现时，取消出现，调用output_hypotheses判断航迹是否满足条件，输出为正式航迹
   - 进一步调整hypothesis索引的波门大小，确保能够覆盖合理范围的点迹，同时避免过多无效假设的生成


### 2026-03-05
1. **LogicBasedTracker假设索引函数实现**
   - 完成了hypothesis索引函数的实现，主要包括以下步骤：
      - 计算当前假设节点的x,y位置，提取sigma_x和sigma_y
      - 根据预先计算的误差分布函数，确定索引波门的大小，确保能够覆盖合理范围的点迹
      - 依据VX,VY，判断哪些点迹落在索引波门内，并且满足距离门限和多普勒门限的条件
      - 对于满足条件的点迹，生成新的假设节点，并将其添加到hypothesis_tree_中
2. **工具函数**
   - calculate_heading_range()：根据当前假设节点的航向和速度范围，计算出可能的航向范围，用于索引时的角度判断
   - location_to_xy_index()：location_to_bin_index()：将点迹的实际位置转换为x,y索引和bin索引，便于进行索引波门的判断
3. **TODO / 明日计划**
   - 实现hypothesis推导函数extend_hypotheses，实现一到三帧的假设推演
     - 尤其是第四帧需要引入DOPPLER来进行减枝
     - 此外当depth=3的目标将要出现时，取消出现，调用output_hypotheses判断航迹是否满足条件，输出为正式航迹
   - 引入calculate_vr_range()函数，根据当前假设节点推断doppler变化范围(不用考虑中间是否过了最小值，这个交给extend_hypotheses函数实现)，进一步优化假设索引的条件，减少无效假设的生成
     - 现在vr_min,br_max的求取均没有完成，我暂时放到backup里面了，明天写vr_min,vr_max的索引 
   - 代码均没有进行测试，明天需要进行两批次点迹的测试，验证hypothesis索引函数的正确性和性能表现
     - 我总感觉现在这个索引函数在bin不够小的时候不如直接大范围暴力遍历。。。 也许需要写一个暴力遍历的对照函数进行对比

## 2026-03-06
1. calculate_vr_range()函数放到query_nodes_by_points()函数中实现，因为query_nodes_by_points函数逻辑进行了完整的修改
2. node删除了 vrmin vrmax，因为我感觉不如依据下一个点推断来的准确。置信度我也打算删除了，反正最后也不输出
3. **query_nodes_by_points()逻辑修改（主要工作）**
   - 注意到此前的逻辑存在严重的区域重复问题，因此修改了逻辑，先依据heading查找可能的点迹范围
   - 之后，依据x,y上的误差，计算理论的bin范围，同时剔除重复区域。
   - 最后，对于每个具体的点迹，结合vx,vy,doppler的保护计算可能的doppler范围，最后进行索引
   - 我计划同构两个sigma和运动距离计算heading_range，从而推算doppler的最大值最小值
4. 完成了calculate_bin_index_range()，进行解耦。。倒是没什么创新
5. 重写了calculate_heading_range(),现在输出heading_center和range，取代了原先的start,end值
6. hypothesis推导函数extend_hypotheses函数估计要改写成process了，不然process结构太单调了，只有位移和extend？减枝直接在depth=3的时候，在output_hypotheses里面输出得了，算了别想这么多，这个之后再搞
7. **TODO / 明日计划**
   - extrack_hypothesis_from_bins函数实现
   - get_expansion_regions函数实现，在桌面track_project文件夹中有写好的 python代码，可以对于那个优化，但是扩展区域还有进一步去重要做。。这个之后再想吧
   - calculate_vr_range()函数实现，基于最大误差：两个sigma+PROTECTED集合平均数和distance计算heading范围，然后推断vr范围。。走一步看一步吧
   - 索引函数query_nodes_by_points正确性任然没验证

## 2026-03-07
1. **LogicBasedTracker索引函数实现**
   - 完成了query_nodes_by_points()函数的实现，主要包括以下步骤：
      - 依据当前假设点迹的参数，反推航向
      - 依据航向，反推所有可能的prev点所在的位置，记录对应的所有边界值
      - 依据prev点所在位置和边界值，包括heading边界值，索引边界值，径向速度边界值，寻找匹配的假设节点
      - 依据假设节点的匹配情况进行粗筛，添加了置信度和heading重合度参数，用于筛出过多的点迹
      - 删除了get_expansion_regions函数，因为这样可以做到更加精细的索引；
      - 删除了extrack_hypothesis_from_bins,直接放完成了query_nodes_by_points里面了。。可能之后会拆，现在这个函数太长了
      - 删除了calculate_vr_range，现在直接由航向遍历获得。。当然可能没那么准了，之后再调
      - 总之先跑通，为了优化这个破代码浪费太多天了，三天都在想结构体和搜索算法，毫无意义
2. **TODO / 明日计划**
   - （重要） 边界测试，构造近距离点迹，远距离点迹，随机少量点迹，中间大量点迹（尤其是群目标），以及随机点迹的测试场景，验证索引函数的正确性和性能表现
     - 目前感觉这个索引函数的性能可能不太好，尤其是当点迹数量较多的时候，可能会出现过多的假设节点被索引出来的情况，导致后续的推导函数需要处理过多的假设节点，影响性能，因此需要进行详细的测试和分析，看看是否需要进一步优化索引逻辑或者增加一些筛选条件来减少无效假设节点的生成
   - 实现hypothesis推导函数extend_hypotheses，实现一到三帧的假设推演
     - 尤其是第四帧需要对于疑似群目标的情况减枝


## 2026-03-09
1. **LogicBasedTracker假设处理函数实现**
   - 索引函数的优化方案，使其最多扩展为O（子假设允许个数^2）的搜索大小。一定程度上缓解了压力吧
   - extend_hypotheses()函数被改写为preprocess_nodes函数了，extend之后估计没了，改成处理冲突的函数，名字之后再想
   - preprocess_nodes()逻辑存在问题需要修改，现在是只关注一个新假设此前关联点的假设是否源自于同一个点，如果源自于同一个点就减枝，不然直接存放新的假设节点，或者直接输出航迹
2. 新增了回调函数和冲突假设存放区域，通过对应的历史假设关联节点的index索引。回调函数是用来DIY的，进一步过滤错误的航迹
3. 冲突处理逻辑的遗漏分析
   - 当前冲突检测仅考虑“多个假设关联同一个前置点迹的depth相同”的情况，但未充分考虑节点深度不同时的组合关系：
      - 对于 depth<2 的节点，由于创建规则确保同一轨迹点不会产生多个同深度节点，因此无冲突。
      - 对于 depth=2 的节点，可能关联到 depth=0 的新点（该点只会生成一个假设，故无冲突），但同时可能存在多个 depth=2 的假设（源自不同 depth=1 的父节点）以及一个depth=1 的假设。现有冲突处理后，会演变为最多一个 depth=2 和一个 depth=1 的假设，但未明确处理多个 depth=2 的情况。
      - 对于 depth=3 的节点，情况更复杂：一个轨迹点可能同时拥有多个 depth=3 的假设（源自不同 depth=2 的父节点）、多个 depth=2 的假设（源自不同 depth=1 的父节点）以及一个 depth=1 的假设。当前冲突检测仅按前置点迹分组，可能导致同一轨迹点下不同深度的节点混合，需要设计合理的保留/剪枝策略。
    - 潜在问题：目前的冲突分组仅以“前置点迹”为键，未区分节点深度，可能导致本应保留的不同深度节点被误判为冲突，或本应冲突的同深度节点未被正确识别。
    - 我感觉可以下手狠一点，让假设节点尽量少，比如直接只保留最大的depth
4. **TODO / 明日计划**
   - （重要）重新审视冲突处理逻辑，对于第3点。
   - 点迹构造效果还挺好，之后加大靠近群目标数量再看看效果吧
   - （重要） 边界测试，构造近距离点迹，远距离点迹，随机少量点迹，中间大量点迹（尤其是群目标），以及随机点迹的测试场景，验证索引函数的正确性和性能表现
   - 冲突解决函数未实现
   - 准备最后的测试:边界测试，构造近距离点迹，远距离点迹，随机少量点迹，中间大量点迹（尤其是群目标），1000个点迹的BENCHMARK，相关的DEBUG函数也需完善

## 2026-03-12
1. **冲突逻辑处理完毕，现在的策略是**
    - 若有多个假设，满足1、当前假设关联点迹相同；2、此前假设关联点迹相同；则只准留下一个假设
    - 若有多个假设，满足1、当前假设关联点迹不同；2、此前假设关联点迹相同；则计算一个置信度，分配前一个点迹的所有权
    - 任然存在问题，最后输出的时候，只看最新的两个点可能会出现:1111,2221这种情况，也就是说，1的前置关联点迹确实不一样，但是1被复用导致最后还是出现了两条航迹。
    - 一个策略是，彻底禁止点迹复用，这样绝对不可能出现错误关联的情况，但是当两个近距离点迹没有被检测出来的时候，可能会因为这个原因无法起始航迹。这个策略的另外一个优势是可以大大降低计算复杂度。需要构造一个特殊场景来证明这个优势
    - DEBUG函数做了下改进，现在突出了重合的情况
2. **TODO / 明日计划**
    - 冲突处理逻辑设计需要修改，我感觉确实得改，应该遍历所有的结果，把出现父假设中存在两个以上重合点迹的假设给合并了，最多只允许有一个假设的点迹是重合的。
      - 这样的话结构体估计得拆成两部分，depth=2的情况和depth=3的情况。
      - depth=2存在的重复情况是（旧->新）（不重合，重合，重合），（重合，不重合，重合），由于他们的最新点都是重合的，所以这部分在preprocess里面就直接可以去重。preprocess也得修改，把（重合，不重合，重合）给剔除了。最后剩下（不重合，不重合，重合），（不重合，重合，不重合），（重合，不重合，不重合），对于重复的结果，计算MSE瓜分置信度1.0
      - depth=3存在的重复情况是（旧->新）3种（不重合，不重合，重合，重合），（不重合，重合，不重合，重合），（重合，不重合，不重合，重合），也就是说，只要保证最后一个点不重合，必然可以直接输出成航迹，用之前的MSE直接计算结果。重合的话，要依据重合的结果瓜分置信度了
      - 综上，冲突列表可以删除，reprocess估计也要删了，这样就只剩下preprocess了。。太长了，明天直接拆分下完事了
    - 大量点迹测试完了，远距离测试完了，随机少量点击测试完了，群目标还没有，边界测试还没有，近距离点迹还没有，重合情况还没有，BENCHMARK还没有

  ## 2026-04-15
   ……随论文完成了这部分测试，完成了1.0版本的开发
