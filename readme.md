# 航迹起始 | TrackInit - 多目标



# 计划
最终封装为一个完整的类，支持事务处理，能够接收一批点迹数据，输出一批航迹起始信息。支持多种算法选择和参数配置。

## 架构设计
### 计划写的算法
1. **基础算法**  (先拿来比较下看看效果)
   - M/N逻辑确认
   - 最小二乘状态估计
   - 物理约束过滤

2. **霍夫变换家族** (开发中)
   - 标准霍夫变换
   - 概率霍夫变换
   - 约束霍夫变换

3. **RANSAC系列** (计划中)
   - 基本RANSAC
   - 多模型RANSAC
   - 自适应RANSAC

4. **机器学习增强** (实验性)
   - 基于ONNX的轨迹验证
   - 特征工程工具

## 软件框架
### 1. 结构体及不可变参数定义
  - def_init.h 用于定义错对外结构体，系统参数等
### 2. 抽象层
  - TrackInitBse 用于定义所有航迹起始组件的接口
### 3. 组件层
   - HoughSlice 基于DBSCAN的霍夫变换算法，将目标区域基于聚类切片，对每个聚集区域，基于霍夫变换执行航迹起始
### 4. 工具层
   - ObjectPool 通用对象内存池
   - Func_dbscan 聚类算法实现 
### 5. 服务层
   - InitService 航迹起始服务层，占据一个单独的线程，调用接口对外提供服务

# 开发日志
## 2025-12-07 至 2025-12-11
1. **创建def_init.h，用于定义基础数据结构和参数**
2. **SingleTargetMemoryPool功能测试完成**
   - 取代malloc/free，快速分配内存
   - 分配10000个目标：平均6.88303 ms
   - 重用1000次：平均776.633 us

## 2025-12-11 至 2025-12-18
1. **HoughSlice算法核心实现完成**
   - 实现了TrackInitBase抽象基类接口
   - 集成DBSCAN聚类算法用于点迹关联
   - 完成内存池DetectedArea集成，支持动态聚类分配
2. **霍夫变换框架搭建**
   - 定义Slice结构体，支持4批次历史点迹存储
   - 实现vote_area二维数组(角度×距离)用于霍夫投票
   - 配置化参数系统：角度分辨率3°，距离分辨率1km，搜索半径50km
3. **状态码系统设计**
   - ProcessStatus枚举：SUCCESS、POINTS_TOO_DISPERSED、TOO_MANY_CLUSTER等
   - 支持算法链式调用：失败状态码(1001-1100)可切换其他算法
4. **引入AI辅助测试**
   - 创建.github/copilot-instructions.md AI开发指南

## 2025-12-18 至 2025-12-22
1. **完善HoughSlice算法**
   - 引入了github上的项目https://github.com/eleobert/dbscan，依据项目需求对其进行了修改，通过dbscan函数即可调用
2. **两类HoughSlice聚类的处理时间比较**
   - 对比两类聚类的处理时间差异：先判断点迹是否在聚类中心附近再聚类，和先聚类再合并聚类的方法
   - 分别设置为gen_1和gen_2两套算法，回学校了在测试

## 2025-12-22 至 2025-12-26
1. **SingleTargetMemoryPool修改为ObjectPool**
   - 原先的单目标内存池过于臃肿，修改为通用对象池ObjectPool<T>重新进行测试，剔除了handler等不必要的功能，不再支持自动析构
   - 使用deque+unordered_set实现空闲对象管理，适配刚需扩容的场景，但是速度较vector慢
   - 目标分配测试:
     |          benchmark name          | est run time |    mean    |  low mean  | high mean  |  std dev   | low std dev | high std dev |
     | :------------------------------: | :----------: | :--------: | :--------: | :--------: | :--------: | :---------: | :----------: |
     |  容量为1的对象池扩容至10k-测试   |  33.1548 ms  | 330.968 us | 327.947 us | 334.005 us | 15.4725 us | 13.7663 us  |  17.6859 us  |
     | 10k对象的池反复获取释放-强度测试 |  14.292 ms   | 141.74 us  | 140.802 us | 143.074 us | 5.66977 us | 4.13455 us  |  8.27005 us  |
     |   10k对象的池获取指针速度-测试   |  26.5129 ms  | 271.204 us | 265.456 us | 293.216 us | 51.2152 us | 10.7818 us  |  119.695 us  |
2. **HoughSlice算法实现**
   - 粗糙的比较了两类新建聚类的方法，发现先聚类再合并的方法在点迹分布均匀的情况下效果更好，但是对于不同情况还需要进一步测试

## 2025-12-27
1. **Release模式性能测试完成**
   - 使用Catch2基准测试比较gen1和gen2聚类算法性能
   - 测试场景：完全随机点迹、五个均匀/高斯/瑞利分布聚类
   - 结果显示gen1算法（先检查聚类中心再聚类）在聚类场景下性能优于gen2（先聚类再合并），平均运行时间gen1 ~250us vs gen2 ~1.5ms
   - 随机场景下两者性能相近，gen1 ~190us, gen2 ~198us  

## 2026-01-07
1. **完善HoughSlice峰值过滤器**
   - 修改peak_filter函数逻辑，确保只有当所有批次的值均非零时，才计算并返回最小峰值数量
   - 修改了process函数，约束了航向角范围，使其符合速度的物理限制
   - 准备完成TODO中的内容，增加角度和索引转换函数，以及航向判断逻辑

## 2026-01-17
1. **HoughSlice流程打通（聚类→投票→峰值→回溯）**
   - 在 [src/HoughSlice.cpp](src/HoughSlice.cpp) 完成 `process()` 主流程串联：聚类生成、批次点迹投票、峰值提取与回溯生成4点段航迹。
   - 仅在 `current_batch_index == 3` 的切片上执行峰值与回溯，保证4批点迹满足后再输出航迹。
2. **霍夫投票区间与多普勒离散编码**
   - 基于 `doppler/velocity_max` 计算允许航向夹角 `±acos(|v|/vmax)`，以点相对聚类中心方位 `beta` 为中心构造区间，并处理跨 0/2π 的两段情况。
   - 每批次用16位保存速度位：负速映射到 bits 15..8，正速映射到 bits 7..0（静止不计入）。投票时对 `(θ, ρ)` 单元做位或，分批次左移 16 位。
3. **峰值一致性与直线参数恢复**
   - `peak_filter()` 采用四批次16位按位与，只有四批共同速度位才视为有效峰值，返回位掩码。
   - `process_extract_peak_from_hough_space()` 逐位还原 `doppler`，并由索引恢复 `theta`、`rho`，输出候选直线参数集合。
4. **点迹回溯与容差**
   - `process_backtrack_points()` 按候选直线在四批各自点集中搜索匹配点；使用 `RHO_TOL=HOUGHSLICE_RHO_RESOLUTION_KM/2` 与 `DOPPLER_TOL=1.0` 做几何/速度一致性判定，成功则输出4点段航迹。
5. **TODO / 明日计划**
   - 发送航迹：完成 `process()` 中“发送航迹 // TODO”的通信/回调对接。
   - 补充测试：构造含4批点迹的单元/基准测试（含跨 0/2π 航向、正/负多普勒、边界ρ索引等场景）。

## 2026-01-18
1. 霍夫变换切片删除逻辑完善
   - 在 `process()` 函数中，完成对已经执行过航迹生成的切片的删除，确保内存池资源得到有效回收。
2. 霍夫变换区域修改
   - **角度搜索范围修改**
   - 目前把角度的搜索给改了。因为doppler的引入，航向角变成了射线而非直线，所以把角度范围从0-360度改为了0-180度。
   - 现在有点后悔，觉得不如doppler按绝对值写比较好，角度还是保持360度。
   - 上面那句话不对，角度还得是180吧，物理意义明确，能保证原空间和霍夫变换空间一一对应。
   - 那就180定下了，doppler还是保留正负，为了*保证原空间和霍夫空间一一对应*，且内存利用率高
   - **截距范围修改**
  - 截距范围做了修改，由于聚类的引入，实际上区间应该是`2*根号2*RADIUS`。
  - 怕取整和计算问题，干脆扩大了一倍，变成了`4*RADIUS`。
  - 目前只修改了参数定义，后续还需要修改索引转换函数。
  - 索引是从0开始的，而截距是从负值开始的，工应该主要是修改这部分内容
3. **TODO / 明日计划**
   - 截距索引转换函数的重写
   - 位扩展类的实现
   - 昨天的发送航迹功能还没实现。。需要决策下实现方式，一定得去耦合：“发送航迹：完成 `process()` 中发送航迹TODO部分”
   - 单元测试也没写，至少得验证转换函数的功能是否正确，性能最后用GPU去优化比较好

## 2026-01-19 
1. **HoughSlice截距索引转换函数重写**
   - 完成了截距索引与实际值的转换函数，确保索引从0开始，实际截距范围为[-2R, +2R]。
   - 修改了 `process()` 中相关计算逻辑，确保投票和峰值提取使用新的索引映射。
2. **⚠位数改动类**
   - 我想着把doppler的位数从uint64_t改成能自由变长的类，但是和uint64_t这些兼容性不太好，改动太大
   - 记录一下，以后要修改的话需要修改的地方：
     - vote_area的定义
     - 投票的位运算 process_vote_points_in_cluster()
     - 峰值提取的位运算 peak_filter()

## 2026-01-22 至 2026-01-30
1. **HoughSlice发送航迹功能实现**
   - 在 `process()` 函数中完成航迹发送逻辑，使用回调函数 `track_callback_` 将生成的航迹数据传递给外部系统。
   - 已经观测到回调函数被正确调用，航迹数据（观测到航迹成功绘制）
   - 修改了trackvisualizer的结构，现在是clear->draw->creat->add->merge的优先级顺序，避免乱位
2. **HoughSlice单元测试编写**
   - 还在功能性测试阶段，聚类进行了修改，现在对于不归属于任何聚类的点迹，会生成一个新的聚类
   - 现在process_cluster_generation在单点情况下已经完成了测试
   - process_point_for_hough_vote部分已经定位到bug，目前投票情况是一票没投。。用MATLAB写了个小程序验证了霍夫变换的正确性，发现代码里投票逻辑有问题
   - 后面两部分还没验证，明天继续
3. **Func_dbscan函数逻辑修改**
   - 修改了dbscan函数的逻辑，现在对于不归属于任何聚类的点迹，会生成一个新的聚类
   - 这样可以避免点迹被丢弃，确保每个点迹都能参与后续的霍夫投票
   - 然后被使用的点应该是不能被第二个聚类使用。。。。不过使用了也许也行，反正是两个聚类切面
4. **TODO / 明日计划**
    - process_point_for_hough_vote的一票未投需要修改
    - 位扩展类不写了，最近几天先尽快完成这个航迹起始算法的开发和测试

## 2026-02-18
1. **HoughSliceBUG修复**
   - 修复了 `process_point_for_hough_vote()` 中的投票逻辑错误，主要是由于最大速度设置过小导致的
   - 修改了`process_point_for_hough_vote`中的逻辑错误，原代码混淆了 point.xy 和 rel_x, rel_y 的使用
   - MATLAB对应代码的读取错误是由于维度不匹配导致的。。并非弧度，虽然看上去结果很像是维度导致的
   - 修改了当点迹依据DOPPLER推算的角度在合理范围内时，反而会导致投票空间越界的BUG，是由于此时的角度区间会越过180度导致的
   - 存在的问题：霍夫空间中监出点迹过多（大幅提高距离分辨率可以有效改善该问题）。。即便是单个航迹迹也能监测出来大量的航迹（存在重复计数），计划在反向索引的时候，每次完成一条反向索引就删除所有的相关点迹，避免重复索引导致的错误，不过这样也有可能导致错误的航迹侵占正确航迹的点迹，导致正确航迹无法被索引出来。。需要进行对比实验验证一下
   - 存在的问题： 霍夫变换采用过大分辨率会导致数据量爆炸，而且目前尚且没明确增加角度分辨率能否进一步剔除无用点迹，需要进行对比实验。。总体来说我觉得霍夫变换可能不是很可行了，后续我还是希望MHT算法能完成
2. 完善了MATLAB脚本，后续合并到项目中，作为单元测试的一部分，验证整个流程的正确性和性能表现
3. **TODO / 明日计划**
   - 反向索引完成后删除相关点迹，避免重复索引导致的错误
   - 继续完善单元测试，构造包含4批点迹的测试场景，验证整个流程的正确性和性能表现

## 2026-02-19
1. **BitArray类引入**
    - 一个变长类，用来替代uint64_t实现任意位宽的位数组，主要用于霍夫变换中多普勒速度的存储
    - 支持基本的位操作（与、或、非、左移、右移）以及位访问和设置
    - 添加了测试代码，进行了功能测试和性能测试
    - **性能基准**（Release模式，Catch2，100 samples，数据规模：批量字节=128KiB，位/移位/逻辑/拷贝=64KiB）：
      | 操作类型                  |    均值    |   标准差   |     吞吐量     |
      | :------------------------ | :--------: | :--------: | :------------: |
      | **批量写入** (131072字节) | 90.013 µs  |  2.359 µs  | **1.39 GiB/s** |
      | **批量读取** (131072字节) | 51.474 µs  |  1.999 µs  | **2.43 GiB/s** |
      | **设置所有位** (524288位) | 330.506 µs |  5.359 µs  |       -        |
      | **读取所有位** (524288位) | 173.529 µs |  4.438 µs  |       -        |
      | **左移 1 位**             |  7.914 µs  |  0.825 µs  |       -        |
      | **左移 32 位**            |  2.173 µs  |  0.326 µs  |       -        |
      | **右移 1 位**             |  7.537 µs  |  0.371 µs  |       -        |
      | **右移 32 位**            |  2.169 µs  |  0.127 µs  |       -        |
      | **位与操作**              | 970.475 ns | 123.557 ns |       -        |
      | **位或操作**              | 946.663 ns | 70.246 ns  |       -        |
      | **拷贝构造**              |  0.188 ns  |  0.012 ns  |       -        |
      | **赋值操作**              |  0.188 ns  |  0.011 ns  |       -        |
2. **HoughSlice精度改进**
   - 将速度存储位宽从 16 位扩展至 256 位（每批次 64 位），速度分级从 ±8 级提升至 ±32 级（引入了bitarray和相关宏，可以自定义）
   - 优化了反向索引逻辑，显著减少了速度索引错误
   - 发现增加角度分辨率只会增加霍夫空间的维度，并不会剔除无用点迹，只会让无用点迹更多，急需要一种方法来剔除无用点迹，靠反向索引是行不通的，必须得依赖霍夫空间点迹凝聚，所以把DOPPLER的位宽增加了
   - 增加RHO分辨率抑制效果很好，不过最好还是依赖于增加批次数，这样也能适当利用霍夫变换本身的性质
   - 准备增加批次数。。
3. **TODO / 明日计划**
  - 反向索引完成后删除相关点迹，避免重复索引导致的错误（待定，我感觉这样太草率了，可能分配到不优秀的航迹里面去反而导致点迹出不来，尤其是两篇HOUGHSLICE重合的时候）
  - 继续完善单元测试，构造包含4批点迹的测试场景，验证整个流程的正确性和性能表现
  - 优先：批次数可以自定义的功能还是得提上来，我认为有了这个能大大压缩点迹数量，不过这样很多地方的逻辑可能要改，显得不像是霍夫变换了

## 2026-02-21
1. **HoughSlice点迹凝聚实现**
   - 关于19日提到的“反向索引完成后删除相关点迹”，已经改成了使用点迹凝聚的思路，故此不在实现“反向索引完成后删除相关点迹”功能
   - 实现了基于角度和距离索引的点迹凝聚算法，将霍夫空间中的相邻峰值合并为一个候选直线参数
   - ⚠19日发现的“增加RHO分辨率抑制效果很好”，主要是抑制了THETA的扩散，并非是RHO有扩散
   - THETA的减小可以有效降低不明点迹的出现
   - 通过调整 `THETA_CLUSTER_TOL` 和 `RHO_CLUSTER_TOL` 参数，可以控制凝聚的严格程度，减少无用点迹的影响
   - 凝聚后的候选直线参数更稳定，反向索引时更容易找到匹配的点迹，提高了航迹生成的准确性
2. **HoughSlice点迹回溯优化**
   - 速度容差不再是绝对值，而是受到velocity_max的控制、根据速度分辨率动态计算，确保不同速度级别的点迹都能得到合理的匹配
3. **HoughSlice航迹生成所需批次数可配置**
   - 19日”批次数自定义功能已经是心啊了“，航迹生成所需的点迹批次数现在受宏控制，支持使用K（K>4）批次的点迹数据来生成航迹
   - 通过调整 `HOUGHSLICE_BATCH_NUM` 宏定义，可以灵活配置生成航迹所需的点迹批次数，大于4时精度提升
4. **TODO / 明日计划**
   - THETA的扩散问题需要进一步的研究，可以进行参数扫描
   -  `THETA_CLUSTER_TOL` 和 `RHO_CLUSTER_TOL`必须受到宏的控制，绝对不能是写死的，会存在今天点迹回溯中出现的适应性问题，进一步提高分辨率可能会导致凝聚效果不理想，降低分辨率又会导致过度凝聚，所以必须受"分辨率宏"的控制
   -  单元测试需要增加更高批次点迹的测试场景，验证批次数提高的效果和性能表现
   -  进一步增加测试点迹，研究边界情况和参数敏感性，确保算法的鲁棒性和适用范围
   -  max_velocity过小的时候存在BUG，部分投票出现在了不可能出现的角度上，需要引起重视

## 2025-02-24
1. **HoughSlice点迹凝聚超参数调整**
   - 优化了点迹凝聚的超参数，确保 `THETA_CLUSTER_TOL` 和 `RHO_CLUSTER_TOL` 受宏控制，并且能够根据分辨率动态调整
   - 解决了航迹回溯的时候存在的点迹搜索不到的问题（两处门限不匹配）
2. **雷达站近距离无峰值检出问题部分修复**
   - 距离雷达站较近的目标DOPPLER速度变化较快，由于不是使用传统的直线投票检出，所以出现了峰值无法检出的问题，现在通过最大速度对该问题进行了一定的改善 
   - 但是DOPPLER投票逻辑显然出问题了，所以明天还得修BUG
3. **TODO / 明日计划**
   - 点迹回溯必须得增加删除机制，不能点迹复用了
   - 增加测试点迹，杂波点迹，准备首位
   - DOPPLER扩展位引入带来了全新的BUG准备修一下

## 2026-02-25 至 2026-02-27
1. **HoughSlice DOPPLER投票逻辑修复**
   - 修复了DOPPLER投票逻辑中的错误，确保根据点迹相对于聚类中心的方位和最大速度计算正确的投票区间，并正确处理跨 0/2π 的情况
   - 现在DOPPLER投票逻辑已经能够正确地将点迹投票到对应的角度和距离单元中
   - 修改了另一处错误，DOUBLE-SIZE和SIZE-DOUBLE不匹配的问题，DOPPLER位现在已经可以确认是匹配的了
2. **HoughSlice点迹回溯逻辑修复**
   - 随机数种子为42，点迹数为3个点的时候，存在峰值检测出来了，但是找不到对应的点迹的问题，需要予以修复
   - 随机数种子为42，点迹数为10个点的时候，存在峰值检测不出来的问题，经过检查时对于THETA忘记取法线方向导致的，在vote_in_hough_space进行修改

准备改DOPPLER容忍度从size_t变成double，实现权责分离
heading情况已经完全完成了，接下来依据heading的情况，修改theta的索引转换函数